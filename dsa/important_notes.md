# Important Notes:

## Strings
- Substring
    ```cpp
    s.substr(startingIndex, length);
    ```
    
## Priority Queue
- Custom Comparator
    ```cpp
    struct GreaterCmp {
        bool operator()(T a, T b)  {
            return a > b;
        }
    };
    ```
